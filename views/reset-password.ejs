<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Réinitialisation de mot de passe - JobFinder</title>
    <style>
        /* Style CSS inclus ici pour simplifier */
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f4f4f4;
            color: #333;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        #reset-password {
            background: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 2rem;
            width: 100%;
            max-width: 400px;
            box-sizing: border-box;
        }

        #reset-password h2 {
            font-size: 1.5rem;
            color: #333;
            margin-bottom: 1rem;
            text-align: center;
        }

        .error-message {
            color: #d9534f;
            background-color: #f2dede;
            border: 1px solid #d9534f;
            padding: 0.75rem;
            border-radius: 4px;
            margin-bottom: 1rem;
            text-align: center;
        }

        #reset-password-form {
            display: flex;
            flex-direction: column;
        }

        .form-group {
            margin-bottom: 1rem;
        }

        .form-group label {
            display: block;
            font-size: 0.875rem;
            color: #555;
            margin-bottom: 0.5rem;
        }

        .form-group input {
            width: 100%;
            padding: 0.75rem;
            border: 1px solid #ddd;
            border-radius: 4px;
            font-size: 1rem;
            box-sizing: border-box;
        }

        /* Style pour les messages d'erreur */
        .invalid-feedback {
            color: red;
            display: none;
            /* Masquer le message d'erreur par défaut */
        }

        .cta-btn {
            background-color: #007bff;
            color: #fff;
            border: none;
            padding: 0.75rem 1.5rem;
            border-radius: 4px;
            font-size: 1rem;
            cursor: pointer;
            transition: background-color 0.3s ease;
            text-align: center;
            display: block;
            width: 100%;
        }

        .cta-btn:hover {
            background-color: #0056b3;
        }
    </style>
</head>

<body>
    <section id="reset-password">
        <div class="container">
            <h2>Réinitialisation de mot de passe</h2>
            
            <% if (typeof error !== 'undefined') { %>
                <div id="passwordError" class="error-message"><%= error %></div> 
            <% } %>

            <form action="/users/reset-password/<%= token %>" method="POST" id="reset-password-form">
                <div class="form-group">
                    <label for="password">Nouveau mot de passe:</label>
                    <input type="password" id="password" name="password" required>
                </div>
                <div class="form-group">
                    <label for="confirmPassword">Confirmer le mot de passe:</label>
                    <input type="password" id="confirmPassword" name="confirmPassword" required>
                </div>
                <button type="submit" class="cta-btn">Réinitialiser le mot de passe</button>
            </form>
        </div>
    </section>


    <!-- Incluez votre fichier JavaScript externe -->
    <script src="/routes/reset-password.js"></script>
</body>

</html>